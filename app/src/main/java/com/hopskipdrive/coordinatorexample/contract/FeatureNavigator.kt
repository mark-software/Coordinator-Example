package com.hopskipdrive.coordinatorexample.contract

/**
 * Created by Mark Miller.
 *
 * This navigator launches different, common flows of the app.
 * For example, update credit card, schedule ride, etc.
 * It may not always be necessary.
 *
 * This would also prob be specific to any subset of features.
 * In other words, not a single global interface for all features.
 */
interface FeatureNavigator {
//    fun goToAddMoney(accountId: String)
//    fun goToTransactionDetails(transactionId: String)
    //common goTo functions
}
